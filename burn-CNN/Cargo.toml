[package]
name = "burn-CNN"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "~0.21", features = [
    "std",
    "tui",
    "train",
    "vision",
    "wgpu",
    "fusion",
], default-features = false }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
